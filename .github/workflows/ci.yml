name: CI

on:
  push:
    branches: [main, develop, feat/**, fix/**, refactor/**]
  pull_request:
    branches: [main, develop, feat/**, fix/**, refactor/**]

jobs:
  backend-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Build shared
        run: npm run build --workspace=packages/shared

      - name: TypeScript check (backend)
        run: npx tsc --noEmit
        working-directory: packages/backend

  frontend-check:
    runs-on: ubuntu-latest
    # Skip until packages/frontend is created in Phase 3
    if: false
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Build shared
        run: npm run build --workspace=packages/shared

      - name: TypeScript check (frontend)
        run: npx tsc --noEmit
        working-directory: packages/frontend

      - name: Build frontend
        run: npm run build
        working-directory: packages/frontend

  security-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Root audit
        run: npm audit --audit-level=high || true
