{
  "name": "vimeo-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Auto-growing chatbot powered by Vimeo video transcripts",
  "workspaces": [
    "packages/shared",
    "packages/knowledge-engine",
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {
    "miyabi": "file:../Miyabi/packages/cli",
    "agent-skill-bus": "file:../agent-skill-bus"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
